<div class="columnRow ogrenciRow">
  <!-- <div class="d-flex justify-content-between align-items-center flex-wrap">
    <div class="word-break">
      <h5><%= ogrenci.ad + " "+ ogrenci.soyad %></h5>
      <span><%= ogrenci.no %></span>
    </div>

    <div class="d-flex align-items-center gap-3">
      <div class="profileImage" style="width: 70px; height: 70px;">
        <img src="<%= ogrenci.profileImage %>" alt="">
      </div>
      <button class="button button-primary ogrenciDetayBtn" value="<%= ogrenci._id%>" data-bs-toggle="modal" data-bs-target="#ogrenciDetayModal">Detay</button>
    </div>
  </div> -->

  <div class="d-flex justify-content-between align-items-center row">
    <div class="col-7 flex-wrap">
      <h5><%= ogrenci.ad + " "+ ogrenci.soyad %></h5>
      <span><%= ogrenci.no %></span>
    </div>

    <div class="d-flex align-items-center gap-3 col-5">
      <div class="profileImage">
        <img src="<%= ogrenci.profileImage %>" alt="">
      </div>
      <button class="button button-primary ogrenciDetayBtn" value="<%= ogrenci._id%>" data-bs-toggle="modal" data-bs-target="#ogrenciDetayModal">Detay</button>
    </div>
  </div>
</div>